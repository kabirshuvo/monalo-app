import ProfileScreen from '@/app/dashboard/profile/ProfileScreen'

export const metadata = {
  title: 'Profile - MonAlo',
}

/** Same profile as /dashboard/profile — canonical URL is /profile */
export default ProfileScreen
